# Content templates (moved)

Canonical templates (blueprints, SEO meta, CSV schemas, brand-discovery questions) now live under **`.ai/templates/`** at the repository root.

This folder is kept as a navigation stub so older docs that pointed at `.ai/workspace/07-content-templates/` still lead somewhere useful.

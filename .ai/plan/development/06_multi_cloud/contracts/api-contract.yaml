# 🛰️ API CONTRACT: Multi-Cloud Sovereignty Relay
# Phase: 6 | Status: DRAFT

endpoint: "https://gateway.aiwf.io/v1"
auth: "mTLS + P2P Signature"

methods:
  - name: "spawn_shadow_runner"
    params:
      provider: "aws | digitalocean | hetzner"
      region: "string"
      workspace_slug: "string"
    returns:
      runner_id: "string"
      endpoint: "string"

  - name: "shard_data"
    params:
      payload: "object"
      sovereign_region: "string"
    description: "Shards sensitive data fields to remain on sovereign soil."

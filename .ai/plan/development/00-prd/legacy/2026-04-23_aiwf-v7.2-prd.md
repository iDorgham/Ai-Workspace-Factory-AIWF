# 🏭 PRD: AIWF v7.2.0 — Sovereign Synchronization & Compliance
**Version:** 7.2.0 | **Author:** Spec Architect | **Status:** DRAFT | **Date:** 2026-04-23

---

## 1. Executive Summary
The primary goal of v7.2.0 is to solve "Fragmented Workspace Debt." Currently, old workspaces (v5/v6) lack the advanced v7 features (Omega Gate v2, Regional Adapter, Scaffold). v7.2.0 introduces the **Hot-Sync Engine**, allowing the factory to propagate core updates safely into client environments while preserving sovereign isolation.

---

## 2. Requirement IDs (REQ)

### [REQ-SYNC-001] — Hot-Sync Protocol
- **Description**: Implement `/sync` command to propagate library updates to existing workspaces.
- **Acceptance Criteria**:
  - Must not overwrite client-specific logic or data.
  - Must create a snapshot branch `sync/v7.2.0-{slug}` before applying.
  - Must validate `.ai/` integrity after sync.

### [REQ-SYNC-002] — Auto-Scaffolder Retrofit
- **Description**: Automatically detect missing `docs/` categories or `.env` files in old workspaces and provision them.
- **Acceptance Criteria**:
  - 100% success rate on retrofitting `docs/00-06` folders.
  - No data loss in existing `plan/` or `.ai/` directories.

### [REQ-SYNC-003] — MENA Compliance Auditor
- **Description**: A T1 sub-agent that scans workspace code for Law 151/2020 (Egypt) compliance.
- **Acceptance Criteria**:
  - Detects PII storage locations.
  - Validates RTL layout implementation.
  - Checks if Fawry/Vodafone Cash env vars are correctly set.

### [REQ-SYNC-004] — Omega Gate v2.1 (Multi-Workspace)
- **Description**: Extend Omega Gate to handle global library promotions from any workspace.
- **Acceptance Criteria**:
  - `/promote [component]` command to move a workspace feature to `factory/library/`.
  - Requires 3-agent consensus + Dorgham Approval.

---

## 3. Technical Constraints & Design
- **Sovereign Isolation**: Syncing MUST be one-way (Factory → Workspace) unless `/promote` is explicitly used.
- **Rollback**: Every sync operation must be reversible via `git revert`.
- **Latency**: Syncing 13 workspaces should take < 3 minutes.

---

## 4. User Flows

### Flow A: The Legacy Upgrade
1. User runs `/sync --all --profile=ultra-antifragile-orchestrator`.
2. System creates branches in all 13 workspaces.
3. System provisions new `.ai/` commands, `docs/` folders, and `AIWF-Guide.md`.
4. System outputs summary of changes per workspace.

### Flow B: Feature Promotion
1. Developer builds a great "Fawry Refund" component in `workspaces/finaltest`.
2. Developer runs `/promote components/payments/fawry-refund`.
3. Omega Gate v2 triggers consensus check.
4. Component is versioned and moved to `factory/library/11-industry-verticals/`.

---

## 5. Success Metrics
- **Upgrade Rate**: 100% of workspaces on v7.x within 24 hours of release.
- **Promotion Velocity**: 5+ new library components promoted per month.
- **Compliance Errors**: 0% PII leak detected by MENA Auditor.

---

*Generated by AIWF Spec Architect*
*Reasoning Hash: sha256:prd-v7.2-2026-04-23*

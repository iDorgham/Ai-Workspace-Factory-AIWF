# Operator entrypoints (`.ai/scripts/bin/`)

Thin wrappers next to canonical scripts under `.ai/scripts/`. Prefer **`bash .ai/scripts/factory_materialize.sh`** in docs; the symlink is optional sugar.

| File | Target |
|------|--------|
| `materialize.sh` | `../factory_materialize.sh` — interactive workspace materialization (`/mat`). |

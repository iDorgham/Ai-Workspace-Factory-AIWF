# Factory runtime reports

Moved from **`factory/reports/`** so all human-facing documentation stays under repo-root **`docs/`**.

Typical files: `swarm_state.json`, `headless_executions.log`, `registry_repairs.log`, `equilibrium_baseline.json`, `sovereign_registry.json`, and `*-composition.md` exports.

# Planning documents (`docs/planning/`)

SDD-style plans, phased automation specs, and fix-plan vectors. Paths in **`.ai/plan/_manifest.yaml`** reference files here via `docs/planning/...`.

**Legacy / version PRD snapshots** live under **[`../archive/planning-legacy/`](../archive/planning-legacy/)**. The **v20 evolution bundle** was moved to **[`../archive/planning-v20-evolution/`](../archive/planning-v20-evolution/)** to keep this folder focused on active work.

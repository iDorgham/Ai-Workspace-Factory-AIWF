# Clients shard tier (MENA-LOCKED)

Production client workspaces live as **local-only** slugs under this directory (not committed). Each shard should ship its own `.ai/` registry and `metadata.json` per `workspaces/README.md`.

**Governance:** No personal R&D artifacts here — use `workspaces/personal/` for incubation.

---
*See `workspaces/README.md` for galaxy-tier routing.*

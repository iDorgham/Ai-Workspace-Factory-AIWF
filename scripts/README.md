# Root convenience scripts

Short wrappers and entrypoints at repo root level. Canonical implementations stay under `.ai/scripts/` and `factory/scripts/` as documented in `README.md` and `docs/PRD.md`.

| Script | Purpose |
|--------|---------|
| `materialize.sh` | Symlink to `.ai/scripts/factory_materialize.sh` — interactive workspace materialization (`/mat`). |

# AIWF factory tests

Python **`unittest`** / **`pytest`** harness for repo-root tooling (CLI router, orchestration, scraper reliability). Run from repository root:

```bash
python3 -m pytest factory/tests/ -q
```

(Install `pytest` in your environment if `python3 -m pytest` is unavailable.)

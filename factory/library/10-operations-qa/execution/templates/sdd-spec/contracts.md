# Spec: [name] | Phase: [phase] | Version: 1.0

> **Planning truth:** Zod shapes derived from **`plan.md` → Data shape**. **`@ContractLock`** auto-generates / validates then **syncs** canonical schemas to **`packages/shared/src/contracts/[domain].ts`** (filename = domain slug, not necessarily spec folder name).

## Data shape → schema mapping

{{data_shape}}

## Generated Zod (summary)

```typescript
// Mirrors packages/shared/src/contracts/[domain].ts after sync
// {{acceptance_criteria}} — boundary types must satisfy AC IDs
```

## Lock & drift

- Fingerprint: **`.contract-locks.json`** (repo root; created on first lock)

<!-- Auto-generated by /plan. Do not modify unless migrating. -->

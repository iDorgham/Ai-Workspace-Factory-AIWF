# Prompt 05 — Documentation and i18n for [Feature Name]

**Prerequisites:** Feature behavior stable enough to describe.

---

Complete `documentation.md` and ensure all user-facing strings are in **both** locales (EN/AR) per project i18n setup.

**Requirements:**

- Align copy with `@Content` / gov-tech or brand tone from `project-type.md`.
- Update `docs/product/` links if user-facing PRD mirrors exist.

**Output:** paths to docs updated + any new translation files.

# Prompt 03 — Frontend implementation for [Feature Name]

**Prerequisites:** `design.md`; contracts/types available for API calls.

---

Implement UI under `apps/web/` per `structure.md` and `design.md`.

**Requirements:**

- Use design tokens only (no raw hex/px); logical CSS (`ms-`, `me-`, …).
- All user-visible strings via i18n keys (`t('namespace.key')`).
- Accessibility: labels, roles, keyboard paths per `design.md`.

**Output:** list of files changed + key i18n namespaces added.

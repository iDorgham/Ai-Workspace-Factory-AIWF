# shadcn SaaS v21 (Neural Fabric profile)

Operator-facing **Neural Fabric** profile: agent markdown, plan/vault stubs, and skills live under this tree. Canonical **factory/library** mirrors and allowlists live in:

| File | Purpose |
|------|---------|
| [`workspace_bundle.manifest.yaml`](workspace_bundle.manifest.yaml) | Agents, skills, commands, **`library:`** paths (`factory/library/...` + profile-local docs). |

See also `agents/`, `skills/`, `commands/`, `plan/`, `vault/`, `logs/ledgers/`, `templates/`.

**Traceability:** `2026-05-04` — workspace bundle manifest aligned with factory library sources.

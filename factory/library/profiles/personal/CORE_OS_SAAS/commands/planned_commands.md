# Planned commands — CORE_OS_SAAS

Subset of `.ai/commands/commands.md` rows this workspace should **retain** after pruning.

- `/plan blueprint` — high-density specs before build.
- `/plan status` — manifest / phase audit.
- `/dev init` — deps and env wiring.
- `/dev implement` — spec-governed implementation.
- `/dev test` — compliance test pass.
- `/dev fix` — healing loop.
- `/audit health` — structural + industrial health.
- `/audit security` — SAST / secrets posture.
- `/git auto` — silent versioning discipline.
- `/mat` — spawn sibling workspaces from factory (optional in mature shards).
- `/guide` — instructor + SDD guardian mode.
- `/help` — router discovery.

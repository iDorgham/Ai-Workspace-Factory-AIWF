# Planned commands — ASSET_OS_LAB

- `/create image`
- `/design list` / `/design use [pack]` (optional `/design install [pack]` if packs are curated)
- `/review`
- `/plan blueprint` (asset briefs as specs)
- `/guide` / `/help`

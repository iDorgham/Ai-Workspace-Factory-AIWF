# Planned commands — MOBILE_OS_FORGE

- `/plan blueprint` / `/plan status`
- `/dev init` / `/dev implement` / `/dev test`
- `/audit security` / `/audit health`
- `/git auto`
- `/guide` / `/help`

Add `/create` or content-router commands only if this shard ships MDX/docs apps, not pure native.

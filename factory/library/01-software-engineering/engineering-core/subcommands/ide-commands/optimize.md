---
cluster: engineering-core
category: subcommands
display_category: Subcommands
id: commands:engineering-core/subcommands/optimize
version: 10.0.0
domains: [engineering-core]
sector_compliance: pending
---
Run Sovereign command `/optimize images` (optional: `in content/sovereign/` when clarifying scope).

Default asset root: `content/sovereign/` (see `active_project` in `.ai/memory/state.json`; paths are `content/<active_project>/`).

Optimize image assets under that project folder and update image SEO metadata (e.g. `assets-seo.json` in the project content root).

Respond using the workspace command output format with a clear next-step command.

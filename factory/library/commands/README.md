# Commands

Industrial documentation for the AIWF Sovereign Library.

- **Status**: OMEGA-VERIFIED
- **Category**: Industrial Core

## Source mirror

Canonical markdown lives under **`.ai/commands/`**. This tree is filled by **`python3 factory/scripts/core/industrial_mirror_sync.py`** (see `MAPPINGS["commands"]`). Edit `.ai/commands/commands.md` and `.ai/commands/guide.md` first, then run that script so `factory/library/commands/` stays identical.

Router + multi-tool rankings are merged in **`commands.md`** only (no separate `commands-multi-tool` / `commands_multi_tool` files). **`guide.md`** includes Humanization v3.4 (no `guide_humanize.md`).

---
cluster: engineering-core
category: subcommands
display_category: Subcommands
id: commands:engineering-core/subcommands/polish
version: 10.0.0
domains: [engineering-core]
sector_compliance: pending
---
Run Sovereign command `/polish content` (optional: `in content/sovereign/` when clarifying scope).

Default draft root: `content/sovereign/` (see `active_project` in `.ai/memory/state.json` for other projects; paths are `content/<active_project>/`).

Optimize draft Markdown in place under that project folder (website-pages, blog-posts, projects, landing-pages).

Respond using the workspace command output format with a clear next-step command.
